import { MJMLParsingOptions } from "mjml-core";

const mjmlOptions: MJMLParsingOptions = {
    keepComments: false,
    beautify: false,
    minify: true,
    validationLevel: "soft",
};

export { mjmlOptions };
